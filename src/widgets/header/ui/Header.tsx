import React from 'react';
import './index.scss';
import { NavBar } from '../../../features/navbar'

export const Header = () => {
  return (
    <header className='header'>
        <NavBar />
    </header>
  )
}
